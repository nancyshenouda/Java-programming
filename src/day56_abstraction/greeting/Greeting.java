package day56_abstraction.greeting;

public interface Greeting {
// interface methods are already public abstract by default. We don't need to add them, it is added by compiler
     void hi();
     void bye();
}
